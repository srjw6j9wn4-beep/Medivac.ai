[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm install --legacy-peer-deps --prefer-offline=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "NODE_ENV=production node dist/index.cjs"
